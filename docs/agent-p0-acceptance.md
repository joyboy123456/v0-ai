# Agent A0＋P0 实施与验收记录

日期：2026-09-16。范围：A0、A1～A5；执行依据为 [任务拆分](./agent-task-breakdown.md) §2.11。

## 实施基线

- 当前项目基线提交：`65e4c8a`；未创建分支，未提交代码。
- 三份设计文档来自 `.delta/worktrees/5e2ny3q5ba1j/v0-ai/docs/`，原副本保留。
- A0 评审补齐：动作判别、摘要输入、旧账证据缺失、安全账故障处理、精确 legacy 例外、任务依赖与客户端接线。
- 使用锁文件安装现有依赖；`pnpm-lock.yaml` 未修改。
- 修改前现有 Beta 定向测试：33/33 通过；类型检查通过。

最新范围修订：本地使用，沿用供应商 access 生图积分实扣；不增加计费系统、预估/预扣/换算/退款或 BillingEvidence 查询。执行账本仅用于去重、任务恢复和未知状态核实。

## 任务记录

| 任务 | 负责人 | 状态 | 验收证据 |
| --- | --- | --- | --- |
| A0 契约冻结 | 主 Agent | 已完成 | §2.10 八项已评审，§2.11 给出冻结条款 |
| A1 类型与摘要 | 契约 Agent | 已通过本地验收 | 12 项测试、类型检查、定向 lint 通过；四种真实 normalizer 兼容 |
| A2 事件与请求工件 | 事件 Agent | 已通过定向验收 | 13 项测试、类型检查、定向 lint 通过；包含并发、篡改、部分写失败和跨会话隔离 |
| A3 旧账兼容 | 账本 Agent | 已通过定向验收 | 39 项迁移/兼容/并发/隔离测试、类型检查、定向 lint 通过 |
| A4 UNKNOWN 核实 | 账本 Agent＋主 Agent | 已通过本地集成验收 | 18 项定向测试＋4 项跨模块集成测试；刷新/确认前接线完成 |
| A5 架构守卫 | 架构 Agent | 已通过定向验收 | 16 项架构与 runner 测试通过（含独立复核补充的后赋值和索引访问负例）；中间全量 91 项通过，最终以集成后复测为准 |

## 测试站与发布边界

- 用户在实施中明确调整节奏：先完成本地开发与本地验收，测试站验收留到后续阶段；不要求现在登录。
- 测试站：`http://121.40.34.214:3100`，PM2 `yibai-preview`，数据 cwd 为 `.preview-runtime`。
- 已做的前置检查仅包括服务器状态和 22 项隔离基线测试；未同步本轮代码、未改服务器业务数据、未发布。
- 故障注入使用临时目录和模拟依赖，不拷贝生产 data，不批量生图。
- 本轮不启用新 Agent 主循环，不运行生产 build，不重启生产。
- 本轮真实生图调用：0；模型/供应商相关测试使用模拟依赖。没有同步本轮代码至服务器，没有执行生产构建或发布。
- 测试站结论：按用户最新要求延期，不记为已通过；后续发布前仍需执行测试站验收。

## 最终统一验收

| 检查 | 结果 |
| --- | --- |
| `pnpm run test:agent` | **124 / 124 通过**，0 失败、0 跳过 |
| `pnpm exec tsc --noEmit --incremental false` | 通过 |
| 受影响 TS/MJS 文件 ESLint | 通过 |
| `git diff --check` | 通过 |
| pnpm-lock.yaml / next-env.d.ts / tsconfig.json 差异检查 | 均无修改 |

统一测试日志：`/tmp/agent-p0-final-tests.lSTIgX`（临时文件，长期依据为本记录与可重复运行的测试）。

主 Agent 在集成时额外验证：

- 未知提交跨 Service 实例和刷新仍只调用一次生成；超过一小时给出人工核实提示。
- 原任务重新可查询后恢复同一个 taskId；任务查询故障不消除已确认创建的证据。
- 安全账核实失败会在创建前中止；会话刷新与重复确认并发无死锁。
- 混合 v1/legacy 账中的未知/在途生成不能被旧入口绕过；节点 taskId 缺失时仍按资产来源隔离。
- 独立审查发现的对象属性后赋值和全局字面量索引绕过均已补上负例，架构测试共 16 项。

## 使用与兼容说明

- 主目录三份设计文档已同步；源隐藏工作区副本保留。
- 旧执行数组先只读兼容；首次安全账写入才保留原始字节备份并升级。无可信新主文件时拒绝执行，旧备份只供人工核实，不会自动覆盖较新的调用证据。
- UNKNOWN 核实只查询任务事实，不查询消费记录，不自动创建、取消、退款或重试。CONFIRMED 仅证明任务创建发生，不代表供应商已扣积分。
- 事件与请求工件 API 已实现，供后续 C9 主循环使用；本轮未切换旧文本规划器到新主循环。
- 架构守卫覆盖静态 AST 和文件内符号流；不是全程序沙箱。

## 最终结论

**A0＋P0 本地实施与验收通过。** A1～A5 均已完成本轮约定范围；P1 感知和 P2 工具/主循环尚未开始，测试站与生产发布按用户节奏另行安排。
