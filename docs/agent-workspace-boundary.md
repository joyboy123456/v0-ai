# Agent 开发工作区与集成边界

> 当前进度已推进至 C9/C13/C12 全部本地验收并集成，最终 880/880 与 19 项模拟 API 浏览器检查通过。本文下方“本次核对”保留最初修订边界时的历史事实；最新状态与各 CLI 阶段副本见 [会话交接](./agent-session-handoff.md)。主项目为集成基线、Codex 跨目录审核、外部 CLI 独立副本的角色边界继续有效。

2026-09-17，按用户本次指示核对并澄清。本文覆盖旧文档中将“不得访问原项目”“唯一工作副本”泛化到 Codex 主会话的表述；不改变产品实现契约。

## 分工与目录

- Codex 负责拆分任务、固定接口与文件白名单、向 kiro-cli / Claude Code 派发任务、审查、返工要求、独立验收及集成。编码 CLI 负责所领任务的实现、测试、修复与开发文档。C8/C9 已有交付仍归 Kiro，不因恢复总体协作模式而转交或重复派发；后续任务由 Codex 明确指定执行者，多个执行者不得共享可写副本。
- 主项目 `/Volumes/DevDisk/Projects/xinman/dianshang/v0-ai` 是集成目标和已验收代码基线；Codex 可读取、比较并在审核通过后按白名单集成。用户原有未提交/未跟踪文件必须保留。Git 是否已提交不决定本地是否已集成。
- `/Volumes/DevDisk/AgentStaging/dianshang-kiro-q56u5x1n/source` 是现有 Kiro 候选交付及独立验收来源，不是整个项目的唯一权威工作区。外部 CLI 仅访问获派隔离副本，不直接访问主项目、旧 TMP、`.env`、业务数据、生产素材或 Git。
- 隔离目录父级保存 baseline、派发文本、测试日志、集成清单及回退副本，供 Codex 核对；这些不是产品文件，不集成到源码。Codex 可读取主项目、候选及这些审计材料；本约定不修改 OS sandbox、认证或权限。
- 本轮保持现有候选副本及其历史证据不变。本次边界修订保存在主项目；后续派发时 Codex 必须将本文作为新的角色/路径约束传入，明确覆盖候选文档中的旧泛化限制。

## 本次核对到的实际进度

在本次文档修订前，主项目的 C8 集成清单 **19/19 文件 SHA-256** 全部匹配 `c8-integration.json` 的 `after`；`baseline-c9.json` 中 **363/363** 既有文件摘要也全部匹配主项目。说明 C8 已在此前完成本地集成，主项目停留在 C9 开工基线。

父目录 `c8-codex-main-tests.log` 记录 698/698 通过、0 失败；`c8-integration.json` 和 `c9-01-prompt.txt` 记录 C8 独立验收与正式移交 C9。主项目旧交接页仍写“C8 待验收”，原因是集成时复制了更新前的文档，后续状态修订留在候选副本。本次只核对既有证据及当前摘要，没有重新执行 C8 测试。

候选副本已包含 C9；其 `docs/agent-c9-acceptance.md` 报告 Kiro 定向 95/95、全量 766/766、类型/lint/架构检查通过，`c9-01.exitcode` 为 0。**C9 仍待 Codex 独立验收，未集成主项目。** 不因自测或退出码而视为通过，也不将整份候选副本覆盖回主项目。当前已有 20 张任务卡验收完成，下一关是 C9 独立验收，随后按依赖推进 C13、C12。

## 集成协议

1. 派发前记录主项目文件摘要、候选来源、验收条件和可修改文件清单。C9 继续使用原 `baseline-c9.json`；历史基线不改写。
2. 审核候选的新增、修改和删除文件，逐项对应白名单；审查语义并独立执行要求的本地检查。缺陷返工给原编码执行者。
3. 集成前再次比较主项目与派发基线；出现并发改动时逐文件保留、合并及复核，禁止整目录覆盖或强制还原。未跟踪文件同样需要基线保护。
4. 仅集成已验收文件，记录来源与目标前后摘要，保留可恢复的旧内容；复核集成后必要检查，再同步主项目进度页。候选自测、独立验收、已集成分别记录。
5. 本次 Codex 文档修订涉及 `AGENTS.md`、本文件、`docs/agent-session-handoff.md`、`docs/agent-task-breakdown.md`、`docs/agent-c8-acceptance.md`。未来 C9 集成必须保留这些主目录修订，不能用候选旧边界覆盖；其余产品代码本轮未修改。

继续只在本地推进；不新增计费、不 build、不操作服务器/测试站或真实产品供应商 API、不创建分支/提交/push。测试站与生产发布仍按项目原流程另行执行。
